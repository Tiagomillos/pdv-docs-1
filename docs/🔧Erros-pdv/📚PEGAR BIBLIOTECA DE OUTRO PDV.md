entre no outro pdv via PUTTY
entre na  /Zanthus/Zeus/

#comandos 

`cd /Zanthus/Zeus`

Crie uma pasta para jogar la as libs

#comandos 

`mkdir biblioteca64` (ou nome que vc quiser)
`ls`
e procura a pasta lib_u64
de o comando:

#comandos 

`tar -zcvf lib_u64.tar.gz lib_u64`
`cp -r lib_u64.tar.gz /biblioteca64`